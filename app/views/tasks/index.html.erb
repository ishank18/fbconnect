<h1>All Tasks</h1>
<table>
	<tr>
		<th>name</th>
		<th>project</th>
		<th>action</th>
	</tr>
	<% @tasks.each do |task| %>
	<tr>
		<td><%= task.name %></td>
		<td><%= task.project.name %></td>
		<td><%= link_to "Edit", edit_task_path(task) %></td>
	</tr>
	<% end %>
</table>
